const requestInstance = require('./requestInstance')
const checkStatus = require('./checkStatus')
const removeInstance = require('./removeInstance')
const checkInstances = require('./checkInstances')

module.exports = {
  requestInstance,
  checkStatus,
  removeInstance,
  checkInstances,
}
