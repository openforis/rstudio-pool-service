{
  "name": "rstudio-pool-service",
  "version": "1.0.0",
  "description": "## Purpose",
  "main": "index.js",
  "directories": {
    "doc": "doc"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/openforis/rstudio-pool-service.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/openforis/rstudio-pool-service/issues"
  },
  "homepage": "https://github.com/openforis/rstudio-pool-service#readme",
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "src/**/*.{js,jsx}": [
      "eslint . --fix", "git add"
    ],
    "src/**/*.scss": [
      "stylelint --syntax scss --fix", "git add"
    ]
  },
  "devDependencies": {
    "eslint": "^7.11.0",
    "husky": "^4.3.0",
    "lint-staged": "^10.4.0",
    "prettier": "^2.1.2"
  }
}
